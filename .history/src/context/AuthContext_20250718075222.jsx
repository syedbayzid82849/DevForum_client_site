import { createContext } from "react";

export const AuthContext = createContext(null);

import { createContext } from "react";

// 🔒 AuthContext তৈরি করো
export const AuthContext = createContext(null);
